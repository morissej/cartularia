# Exécution V7 — « Performance, hygiène et recette finale » (16 septembre 2026)

Plan : `docs/audits/2026-09-08-plan-actions-par-vagues.md` § V7 (V-B2, V-B4, V-B5 ; décisions en attente ; recette des 49 constats). Branche `feat/lecteur-unique-adr-028-031`, ouverture à `a4ce595` (V6 déployée). Analyse du 16 septembre : cinq lectures avec mesures sur le build (Chrome headless, HTTP/2 local, tailles brotli), deux propositions par point, trois juges, une critique de cohérence (scratchpad `v7/` : `lecture-*.md`, `proposition-*.md`, `brief-*.md`, `coherence.md`, `statut-49-constats.md`).

## 1. Constat mesuré et périmètre

- V-B2 : 94 morceaux JS (1,8 Mo brut) dont 36 morceaux « icône seule » (45 icônes) + `createLucideIcon-*.js` ; en HTTP/2 le nombre de fichiers pèse ± 10 ms : les 6 à 8 s d'ouverture du Registre démo relèvent du second document (rechargement complet), de l'initialisation Firebase et App Check et des 27 canaux Firestore Listen (mesure V3), non du découpage. Le regroupement des icônes et le préchargement du Registre depuis la page de connexion restent utiles (étape Registre à 0 JS réseau à chaud) mais V7 ne promet pas de secondes.
- V-B5 : trois polices Google (Archivo, JetBrains Mono, Newsreader), deux connexions TLS tierces, 198 ko ; hébergement local = trois `.woff2` latin (35 + 31 + 132 ko) + licence OFL.
- V-B4 : aucun routeur client ; accueil → démo → Registre = trois documents ; un routeur unique touche `cartularyIds.ts`, les singletons de `localVault.ts` et App.tsx : reporté.
- Décisions en attente : (a) manifeste automobile — le statut « active » n'est lu ni par le client ni par le serveur (règle `latest` : `car@1.2.0`), et la production n'a aucun Cartulaire automobile (lecture J0 du 16 septembre : `schemaCatalog/car` latest 1.2.0, active null, versions 1.0.0-1.2.0 ; `watch` active 1.6.0 ; 0 cartulaire `car`) ; (b) codes publics en dur dans `cartularyIds.ts` : aucun producteur de `/cartulary?publicCode=`, alias mort ; (c) copies « 2 » : 375 fichiers sources hors git (124 sous `public/assets/IWC/derivatives/`, 128 livrées en production par le build : à retirer de la livraison) ; (d) IWC `watch@1.6.0` : fait en V1 (révision 8), seuls la skill et la mémoire sont périmées.
- Budget `measure:pf0` : rouge hors barrière depuis V5 point 1 (`c078b2d`, 15 septembre 2026, lecture en texte pur et édition par crayon : App 313 273 → 323 275 o ; 309 236 o à la clôture V3, 331 485 o à la clôture V5, 330 028 o en V6 pour 320 000 — builds datés rejoués à la relecture V7).
- Recette : 49 constats des deux audits du 8 septembre ; statut au 16 septembre (avant V7) dans `v7/statut-49-constats.md` : visiteur 20 clos, 3 partiels, 3 ouverts (V-B2, V-B4, V-B5) ; propriétaire : voir § H des audits.

## 2. Décisions

| # | Décision | Retenu |
|---|---|---|
| D1 | Polices : trois `.woff2` latin à déposer dans `src/assets/fonts/` + `OFL.txt` | **Accord donné et fichiers déposés par Jérôme le 16 septembre (J1)** : les trois fichiers que Google Fonts servait (tailles et URL vérifiées par HEAD), un `OFL.txt` par famille (SIL OFL 1.1, aucun *Reserved Font Name*) ; commit C4 livré (§ 4) |
| D2 | Copies « 2 » : archivage hors iCloud par `rsync --remove-source-files` (simulation `-n` d'abord) | Commande remise à Jérôme ; livraison protégée entre-temps par un greffon de build et `hosting.ignore` |
| D3 | Manifeste automobile | A : ne rien faire (verrou `hygiene-v7`) ; J0 fait |
| D4 | Routeur client (V-B4) | Reporté (V8) avec ticket d'entrée : partition mesurée des surfaces navigables en place / liées au document |
| D5 | Codes publics en dur | Retirés (−3 lignes), `readOnlyPreview` explicite, test |
| D6 | Budget `measure:pf0` | Relevé à 340 000 (gzip 92 000), cause datée, rattaché à `verify:v7` |
| D7 | Découpage Vite | Deux groupes `react` + `icons` (`build.rolldownOptions.output.codeSplitting.groups`) |
| D8 | Gardes contre les copies numérotées | Greffon `closeBundle` + `hosting.ignore "**/* +([0-9]).*"` (firebase.json, firebase.personal.json ; relecture V7 : extglob à un ou plusieurs chiffres, équivalent du `\d+` du greffon — le motif initial `"**/* [0-9].*"` laissait livrer « nom 12.ext », seule garde de `dist-personal/`) |
| D9 | Assertion « 0 copie dans public/ » dans `test:v7` | Non (constat par `find` au journal) |
| D10 | Préchargement du Registre | À l'inactivité, 1,5 à 2,5 s après le premier rendu de la page de connexion, annulé au démontage. Risque accepté (R3 du brief bundle, décision 9 : « accepter en V7, neutraliser si observé en recette ») : un échec du préchargement — morceau ou `registry-*.css` disparu après un déploiement, réseau coupé ou changé — déclenche le rechargement `vite:preloadError` de `main.tsx` (un par minute) et vide un formulaire de connexion en cours de saisie ; fenêtre ≈ 1,8-2,5 s après le rendu, parcours démo sans saisie ; repli prévu (V8) : ignorer l'événement pendant le préchargement d'arrière-plan |
| D11 | Coffre personnel | `assetsInlineLimit` + preload + ignore seulement |
| D12 | Mise à jour des audits | Section finale « H. Statut des constats (recette V7) » par audit |
| D13 | Six exceptions axe `until: "V7"` | Reportées V8, datées ; dette « contrôle mécanique de `until` » |
| D14 | Résidu V-A2 (App Check sur toute surface) | Reporté, écrit dans l'audit § H |
| D15 | Documents hors dépôt (skill § 5, mémoire) | Mis à jour |
| D16 | Déploiement | Un seul Hosting groupé après C4 (polices) ; si les polices tardent, décision explicite |

## 3. Ordre d'intégration

C1 découpage react/icons + `dist` sans copie numérotée + `measure:pf0` étendu + `measure:surfaces` + `test:v7`/`verify:v7` → C2 préchargement du Registre → C3 décisions (codes publics, copies jamais livrées, documents (a)-(d)) → [J1 polices par Jérôme] → C4 polices hébergées → C5 fusion (seuils, relevé `docs/audits/perf/`, allowlist axe, journal, section H des audits) → [R0 mesure « avant » de B2 par Jérôme] → J4 déploiement groupé → R1 recette anonyme (assistant) → R2 recette propriétaire et téléphone (Jérôme) → R3/C6 statut des 49 constats.

## 4. Implémentation (16 septembre) — cinq commits, C4 (polices) livré après J1

Ordre réel des commits (l'autre piste a livré C3 avant C1) ; tous sur `feat/lecteur-unique-adr-028-031`, au-dessus de `96a2a36` (ouverture du journal) ; C1-C5 et la relecture poussés, C4 non poussé. Vocabulaire : **mesuré** (relevé par un outil sur `dist/`, Chrome 152 headless par CDP, serveur `node:http` sans compression avec les en-têtes de `firebase.json`, réseau coupé hors `127.0.0.1` — plus Google Fonts jusqu'à C4) ; **vérifié** (lu dans le dépôt) ; **supposé** (déduit).

| Commit | Lot | Contenu |
|---|---|---|
| `d45e959` | C3 décisions (D3, D5, D15) | Codes publics retirés de `cartularyIds.ts` (−3 lignes, ADR-029) ; `readOnlyPreview: isDemoCartulary \|\| isWatchWebsite` (`App.tsx:667`, 0 ligne nette : le mini-site public n'ouvre plus l'écoute `cartularies/{id}/reminders`) ; `tests/hygiene-v7.test.mjs` (verrous (a) manifeste automobile inchangé, (b) mini-site sans écoute des rappels ; (c) `hosting.ignore` vit dans `performance-v7`, (d) constat sans test) ; `interface-state` +15 lignes (cinq codes × trois routes, priorité de `cartularyId`) ; contrat ADR-029 (+1 test) ; documents (a)-(d) : plan § V7.2, ADR-029, `NOTE_PASSATION:357`, `README:58` ; skill `cartularia-dev` § 5 et mémoire hors dépôt (D15) |
| `f5ea262` | C1 bundle (D6, D7, D8, D11) | `vite.config.ts` : deux groupes `codeSplitting` `react` (priorité 2) + `icons` (priorité 1), greffon `cartularia-copies-numerotees` (`closeBundle`, récursif, avertit sans échouer), `assetsInlineLimit` refusant `.woff2` (aussi dans `vite.personal.config.ts`) ; `hosting.ignore "**/* [0-9].*"` dans `firebase.json` et `firebase.personal.json` (mesuré avec `listFiles` de firebase-tools ; élargi à `"**/* +([0-9]).*"` à la relecture, D8) ; `measure:pf0` étendu (`initial`, `app` 340 000 / gzip 92 000 avec cause datée, `javascriptFiles ≤ 65`, `reactChunk`, `iconsChunk`, `iconDefinitionFiles === 1`, `numberedCopies === 0`) ; `scripts/measure-surfaces.mjs` (quatre surfaces × deux fenêtres + parcours à chaud, `--check`, `--fonts`) ; `tests/performance-v7.test.mjs` ; `test:v7` / `verify:v7` |
| `7ce17e9` | C2 préchargement (D10) | `src/features/public/registryPreload.ts` (`import()` de `RegistryApp.tsx` et `RegistryItems.tsx` dans `Promise.allSettled`, `setTimeout` 1 500 ms puis `requestIdleCallback({ timeout: 1000 })`, annulation au démontage) ; `AccountAccessPage.tsx` : `useEffect` gardé par `!creation && requestedSpace !== 'vault' && (demoRequested \|\| returnTo.startsWith('/registry'))` ; `tests/ui/registry-preload.test.tsx` ; `tests/performance-v7.test.mjs` (+2 tests AST : `registryPreload.ts`, `AccountAccessPage.tsx`) ; `tests/ui/account-access-corrections.test.tsx` (+4 : `vi.mock` des deux modules du Registre, M9) ; seuil du parcours à chaud (étape Registre : 0 JS réseau, ≤ 3 requêtes) |
| `5cc9587` | C5 fusion (K8, K9, K13, D12, D13, D14) | `test:v7` définitif (+ `hygiene-v7`) ; seuils de `measure:surfaces` fixés depuis le relevé fusionné ; relevé sans port éphémère ; `tests/performance-v7.test.mjs` (regex `LIMITS` K9, deux assertions « sans port », test de barrière ramené à la forme) ; test « V7 (fusion) » du contrat ; `docs/audits/perf/2026-09-16.{json,md}` ; six exceptions axe reportées à V8, datées ; section H des deux audits ; ce paragraphe |
| `dcc7f78` | Relecture V7 (quatre angles, constats confirmés par deux vérificateurs) | § 4.6 |
| ce commit | C4 polices (V-B5, D1, D11, D16) | `src/assets/fonts/<famille>/` : `archivo-v25-latin-wght.woff2` (34 940 o), `jetbrains-mono-v24-latin-wght.woff2` (31 340 o), `newsreader-v26-latin-opsz-wght.woff2` (131 848 o) + `OFL.txt` (déposés par Jérôme, J1) ; `src/styles/fonts.css` (trois `@font-face` reproduisant la feuille css2 : Archivo 400-700 `font-stretch: 100%`, JetBrains Mono 400-700, Newsreader 500-600, `swap`, `unicode-range` latin), importée par `src/index.css` après `variables.css` (le Coffre hérite par `personalVault/main.tsx`) ; `index.html` et `personal-vault.html` : les deux `preconnect` et la feuille Google remplacés par un `preload` de Newsreader (Vite réécrit le `href` vers `/assets/newsreader-…-DBQoWVJ5.woff2`, même empreinte que l'`url()` de la feuille, vérifié sur `dist/` et `dist-personal/`) ; CSP : `firebase.json` (Report-Only) `style-src 'self' 'unsafe-inline'`, `font-src 'self' data:`, `firebase.personal.json` (appliquée) `font-src 'self'` ; `scripts/measure-surfaces.mjs` : `--fonts` retiré (K1/K9 : résolveur `MAP * ~NOTFOUND, EXCLUDE 127.0.0.1` sans exception), « 0 requête tierce hors App Check » sur chaque surface et chaque étape (`APP_CHECK_HOSTS` = `www.google.com`, `www.gstatic.com`, `recaptchaenterprise.googleapis.com`, résidu V-A2 / D14) et « 3 polices même origine sur l'accueil » (`LIMITS.accueil.polices`), colonne « Tierces hors App Check » ; `package.json` : `measure:surfaces` sans `--fonts`, `test:v7` += `tests/fonts-contract.test.mjs` ; contrat « V7 (fusion) » (K13, chaînes exactes) ; `tests/performance-v7.test.mjs` (résolveur exact, seuil tierce, `LIMITS` avec `polices: 3`) ; `tests/presentation-derivatives.test.mjs` (Google Fonts absent, preload présent) ; `tests/fonts-contract.test.mjs` (nouveau : documents, feuilles, fichiers `wOF2` + `OFL.txt`, CSP, `dist/`) ; `docs/PF4_MEDIA_DERIVATIVES_FONTS.md` ; relevés `docs/audits/perf/` et `docs/audits/a11y/` régénérés ; § H B5 de l'audit navigateur ; ce paragraphe |

### 4.1 Mesures avant / après (V-B2)

Avant = HEAD `a4ce595` (`dist/` = production V6, empreinte `index-DAVpr6jE.js`), lecture bundle du 16 septembre (`scratchpad/v7/bundle-sizes.txt`, `net-*.json` : serveur local brotli, cache froid, 1 440 × 900). Après = build fusionné C1-C3 (`docs/audits/perf/2026-09-16.md`, `scratchpad/v7/c5/pf0-pre.json`). Les octets « avant » des surfaces sont des octets sur le fil en brotli, les octets « après » sont bruts avec gzip recalculé depuis `dist/` : seuls les comptes se comparent directement. Niveaux gzip (relecture) : les gzip par morceau du tableau sont ceux de `measure:pf0` (`gzipSync` sans option, zlib niveau par défaut) ; le relevé `docs/audits/perf/2026-09-16.md` recalcule au niveau 9 (`App-Diikh_Cd.js` 88 356 o = 86,3 ko contre 88 621 ici, `react-*` 58 835 contre 58 945) — le budget `appGzip` se lit au niveau par défaut.

| Mesure | Avant (`a4ce595`) | Après (C1-C3) | Note |
|---|---|---|---|
| Morceaux JS dans `dist/assets` | 94 (dont 36 icônes lucide livrées seules + 1 `createLucideIcon-*`) | 60, 0 icône seule | mesuré ; `javascriptFiles ≤ 65` dans `measure:pf0` |
| JS total (brut / gzip) | 1 822,7 ko (1 866 445 o) / 551,2 ko | 1 814,6 ko (1 858 111 o) / 541,5 ko | mesuré, gzip au niveau zlib par défaut de `measure:pf0` dans les deux colonnes (au niveau 9 de la lecture bundle : 550,1 → 540,3 ko) ; le regroupement n'ajoute pas d'octets |
| Entrée `index-*.js` | 210 244 o | 19 518 o (`index-254gF_q7.js`) | React sort de l'entrée : `react-DHQQvKuQ.js` 189 589 o / 58 945 gzip, empreinte stable entre builds (mesuré sur quatre builds du 16 septembre) — un déploiement applicatif ne l'invalide plus, à partir du déploiement qui suit J4 : la production `a4ce595` n'a ni `react-*.js` ni `icons-*.js`, J4 lui-même renouvelle 37 morceaux ≈ 272 ko brotli au maximum par visiteur de retour (dont `react-DHQQvKuQ.js` 50,7 ko), 29 noms conservés ≈ 245 ko (mesuré à la relecture contre `bundle-sizes.json` de la production) |
| `initial` (entrée + `modulepreload` runtime + react) | — | 209 823 o / 66 521 gzip (budget 250 000) | nouveau contrôle (K7) |
| `icons-*.js` | — | `icons-CaennByB.js` 27 076 o / 8 806 gzip, n'importe que le runtime et `react-*`, importé statiquement par 21 morceaux (22 références : l'entrée ne le cite que dans sa table de préchargement `__vite__mapDeps`) | mesuré ; une seule définition d'icône dans tout `dist/` (`iconDefinitionFiles`). Coût par surface du jeu complet (fermeture statique des imports sur `dist/`, relecture) : les surfaces à 3-4 icônes paient le morceau entier — `/community` (3 icônes) +5,3 ko gzip / +4,8 ko brotli net par rapport à V6, `/collection-website` (4) +4,9 / +4,4, invitation (4) +5,2 / +4,6, accueil +4,7 ko gzip — soit ≈ 2 % de leur JS à froid pour 2 à 4 requêtes de moins et un morceau immuable partagé (arbitrage D7 : requêtes et cache, pas octets) ; ces trois surfaces sont hors relevé `measure:surfaces` |
| `App-*.js` | 330 028 o (budget 320 000 dépassé depuis V5 point 1 `c078b2d`, 15 septembre 2026 : 313 273 → 323 275 o ; hors barrière) | 327 837 o / 88 621 gzip (budget 340 000 / 92 000, dans `verify:v7`) | D6 |
| `RegistryApp-*.js` | 44 716 o | 37 201 o / 10 727 gzip | effet de C3 (codes publics) et du groupe icons |
| Plus gros morceau | `index.esm-DRo0ZM8h.js` 460 963 o | identique | Firestore + App Check, hors périmètre (résidu V-A2) |
| Fichiers dans `dist/` | 554 (dont 124 copies « N » + 4 résidus racine) | 392, 0 copie numérotée (124 retirées par le greffon à chaque build tant que J3 n'est pas faite) | mesuré ; liste de déploiement `listFiles` : 554 → 426 sur le `dist` V6, 392 = 392 sur le build fusionné |
| Accueil `/` (froid) | 21 JS, 33 requêtes (29 même origine + CSS Google + 3 `.woff2`) | 8 JS (296,8 ko brut / 93,5 ko gzip), 20 requêtes dont 4 Google Fonts ; **après C4 : 19 requêtes, 0 distante, 2 feuilles, 3 polices même origine (198 795 o sur le fil)** | identique à 390 ; C4 : § 4.1 bis |
| Connexion `/account/sign-in` (froid) | 18 JS, 29 requêtes | 12 JS initiaux (22 avec les 10 préchargés après le premier rendu), 34 requêtes | le préchargement du Registre s'ajoute après le rendu (C2) |
| Démo `#cover` (froid) | 43 JS, 56 requêtes | 29 JS (1 386,4 ko brut / 404,0 ko gzip), 42 requêtes | — |
| Registre `/registry` déconnecté (froid) | 29 JS, 40 requêtes | 17 JS (963,7 ko / 291,0 ko gzip), 28 requêtes | — |
| Parcours à chaud accueil → connexion → `/registry/reg_cartularia_demo/items` | non mesuré avant V7 (C1, sans préchargement : étape Registre 5 JS réseau, 9 requêtes réseau, 51,5 ko) | étape Registre : 27 requêtes dont 23 servies par le cache, 3 réseau (11,6 ko : `index.html`, logo, manifeste — tous `no-cache`) et 1 refusée (`enterprise.js` d'App Check, DNS coupé), **0 JS réseau** | mesuré, seuil de `verify:v7` ; conditions (relecture) : la connexion attend 3,5 s d'inactivité (`settle: 3500`), le préchargement part à ≈ 1,8 s — un clic démo avant ≈ 2 s coûte les 5 JS de la colonne « avant » — et, sans session en local, l'étape Registre rend le formulaire de connexion (`RegistrySignIn`), pas le catalogue ; les 13 dépendances de la section `items` sont pourtant toutes en cache (vérifié sur `__vite__mapDeps`), le 0 JS tient donc en session connectée (à recetter en R1) |
| Rendu prêt (local) | 384-766 ms (`cdp-network.mjs` : sondage 125 ms, polices chargées exigées, serveur brotli) | 505-513 ms (`measure-surfaces` : 2e sondage de 250 ms + latence CDP) | instruments différents, non comparables (relecture) : `measure-surfaces` rejoué sur le `dist` a4ce595 donne 507-512 ms, et un sondage fin place les deux builds prêts à ≈ 330-380 ms ; DCL 24-76 ms, LCP 360-444 ms informatifs ; bruit local, jamais contrôlé |

Effet du nombre de fichiers sur la durée (lecture bundle § 3.3, HTTP/2 local, latence émulée 150 ms) : démo 43 → 28 scripts, dernier JS à 541 → 526 ms ; Registre 29 → 16, 528 → 534 ms. Le nombre de morceaux ne pèse que ± 10 ms en HTTP/2 : les 6-8 s du constat B2 ne viennent pas du découpage.

#### 4.1 bis Mesures avant / après C4 (V-B5, polices hébergées)

Avant = build fusionné C1-C3 + relecture (`dcc7f78`, relevé `docs/audits/perf/2026-09-16.json` avec `--fonts`, copie dans `scratchpad/v7/c4/head/`) ; après = ce commit (même relevé régénéré par `verify:v7`, réseau coupé sans exception). Mesuré sur `dist/`, Chrome 152 headless, 1 440 × 900 et 390 × 844 (identiques).

| Mesure | Avant C4 | Après C4 | Note |
|---|---|---|---|
| Polices | feuille `fonts.googleapis.com/css2` (1 150-1 498 o, bloquante pour le rendu, `private, max-age=86400`) + 3 `.woff2` `fonts.gstatic.com` (198 236 o sur le fil) ; 42 faces déclarées, 8 chargées | 3 `.woff2` même origine sous `/assets/**` `immutable` (198 795 o sur le fil, 198 128 o de fichiers : 34 940 + 31 340 + 131 848), `document.fonts.size === 3`, 3 faces `loaded` ; aucune feuille tierce | mesuré ; mêmes octets de police (fichiers identiques à ceux que Google servait, sha256 § 4.5) |
| Accueil `/` (froid) : requêtes / distantes / feuilles / polices | 20 / 4 / 3 / 3 distantes | **19 / 0 / 2 / 3 même origine** | mesuré ; 8 JS inchangés ; seuils `accueil ≤ 22 requêtes`, `polices: 3`, 0 tierce |
| Connexion, démo `#cover`, Registre (froid) : requêtes / distantes | 34 / 5, 42 / 5, 28 / 5 | 33 / 1, 41 / 1, 27 / 1 — la seule distante est `www.google.com/recaptcha/enterprise.js` (App Check, refusée en DNS : résidu V-A2, hors seuil par `APP_CHECK_HOSTS`) | mesuré ; JS inchangés (22 / 29 / 17) |
| DCL local (accueil / démo / Registre) | 76 / 34 / 26 ms | 17 / 16 / 16 ms (disparition de la feuille tierce bloquante) | mesuré, bruit local, non transposable en absolu |
| Parcours à chaud, étape Registre | 27 requêtes, 3 réseau (11,9 ko), 0 JS, 4 polices Google en cache + `www.google.com` refusé | 26 requêtes, 3 réseau (11,6 ko), 0 JS, `www.google.com` refusé seul | mesuré ; seuil inchangé |
| Chronologie de Newsreader (police du `h1`), accueil, latence émulée 100 ms, HTTP/1.1 local, 3 rejeux (`scratchpad/v7/exp-fonts/timing-fonts.mjs`, sorties `c4/timing-{avec,sans}-preload.json`) | sans `preload` (variante de `dist/` sans la balise) : demandée après l'analyse de la feuille, à 241-249 ms, reçue à 447-458 ms, après le premier rendu (FCP 256-268 ms) : bascule `swap` visible | avec le `preload` de `index.html` : demandée avec la feuille, à 111-116 ms, reçue à 222-225 ms, **avant le premier rendu** (FCP 260-268 ms) : `h1` peint directement en Newsreader ; −234 ms (médianes 457 → 223) | mesuré, local ; le −130 ms du brief (HTTP/2, proposant) allait dans le même sens |
| `measure:pf0` | `App-Diikh_Cd.js` 327 837 o ; `fonts.woff2` 0 | `App-CBFhLMjQ.js` 327 837 o (même contenu, empreinte renouvelée par la feuille) ; `fonts.woff2` 3 ; CSS 6 fichiers 314 170 o | mesuré, tout vert |
| Empreintes renouvelées par C4 (cascade de `index-*.css`) | — | dans le relevé des quatre surfaces : 7 fichiers sur 41 (`index-0etb8sXI.css`, `index-CBFIhEXE.js`, `App`, `HomePage`, `RegistryApp`, `AccountAccessPage`, `PrivateMediaImage`), 34 conservés dont `react-DHQQvKuQ.js` et `icons-CaennByB.js` ; `dist/` 395 fichiers (392 + 3 `.woff2`) | mesuré ; confirme D16 (déploiement groupé : une seule invalidation) |
| `audit:a11y` (voit désormais les vraies polices) | 0 bloquante, 0 assertion en échec, 419 nœuds à vérifier | 0 bloquante, 0 assertion en échec, **420** nœuds à vérifier | mesuré ; la différence est analysée § 4.4 (aucune exception ajoutée) |
| Coffre personnel (`npm run build:personal`, hors barrière) | feuille Google dans `personal-vault.html` | `preload` réécrit vers la même empreinte `newsreader-…-DBQoWVJ5.woff2`, 3 `.woff2` émis sous `dist-personal/assets/`, aucune police en `data:` | vérifié sur `dist-personal/` |

### 4.2 Requalification du constat V-B2

- **Nombre de fichiers : clos** (icônes regroupées, 94 → 60 morceaux, étape Registre à 0 JS réseau depuis la page de connexion).
- **Durée d'ouverture (6-8 s) : imputée** au second document (`/account/sign-in` → `/registry/…/items` par `window.location.assign`, aucun routeur), à la ré-initialisation de Firebase et d'App Check (reCAPTCHA Enterprise, `enterprise.js`) et aux 27 canaux Firestore Listen (mesure V3 § 13.2) — pas au découpage. Cette part n'est mesurable qu'en production dans un Chrome réel (score reCAPTCHA) : R0 (avant déploiement) puis R2 (après), même protocole (§ 4.4 de la cohérence : chrono clic → catalogue, `/assets/*.js` réseau, canaux Listen, `enterprise.js`, cache vidé puis chaud, × 3).
- **Suite : V-B4 reporté (V8)**, décision reprise après R0/R2 avec le ticket d'entrée ci-dessous.

### 4.3 Partition des surfaces (ticket d'entrée de V-B4, D4)

Parcours statique des imports depuis chaque surface de `RootPage.tsx` (`scratchpad/v7/reach-static.mjs`, rejoué le 16 septembre sur le build fusionné ; `import()` ignorés). Modules « liés au document » : `cartularyIds.ts` (`ACTIVE_CARTULARY_ID` figé à l'import), `localVault.ts` (singletons), `activeCartulary.ts`, `useHybridPersistence.ts`, `codeHandoffCapture.ts`. `firebase.ts` est atteint par toutes les surfaces connectées : c'est un singleton partageable entre documents, pas un obstacle.

| Groupe | Surfaces (modules statiques atteints) | Verdict |
|---|---|---|
| Navigables en place sans refonte | `home` (7), `service-information` (2), `account-sign-in` / `account-create` (9, via `firebase.ts`), `account-recovery` (8), `registry` (24) et `registry/items` (14), `invitation` (8), `community` (18), `collection-website` (13), `administration` (11), `not-found` | aucun module lié au document |
| Liées au document | `cartulary`, `cartulary-demo`, `cartulary-view`, `watch-website` (`App.tsx`, 108 modules : `cartularyIds.ts` ← `localVault.ts` ← `useCartularyFollowUp.ts`, `activeCartulary.ts`, `useHybridPersistence.ts`) ; `personal-vault` (22) et `code-handoff-return` (4) (`codeHandoffCapture.ts`) | rechargement complet obligatoire tant que `ACTIVE_CARTULARY_ID` et les singletons de `localVault.ts` ne sont pas résolus à l'exécution |
| Cas mixte à instruire | `registry/new` (`NewCartularyPage.tsx`, 22 modules) atteint `cartularyIds.ts` par `localVault.ts` ← `cartularyCreation.ts` | l'option minimale « famille publique + Registre » doit soit exclure la page de création du routage en place, soit sortir `cartularyCreation.ts` de `localVault.ts` |

Options pour V8 (proposition polices-routeur structurelle § lot C) : S-M, racine réactive pour la famille publique + Registre (`navigateApplication` unique, `key={route}`), accueil → démo → Registre reste à trois documents (le Cartulaire est lié au document) ; L, routeur unique conditionné à la sortie des singletons de `cartularyIds.ts` / `localVault.ts`, à instruire avec ADR-029 (même fichier). Aucune des deux n'est mesurable en local (App Check).

### 4.4 Décisions appliquées, barrières, dettes

- Appliquées : D1 (C4 : polices déposées par Jérôme le 16 septembre, J1 ; `fonts.css`, `preload`, CSP, `--fonts` retiré, « 0 requête tierce » et « 3 polices même origine » dans `verify:v7`), D3 (A, J0 consigné § 1), D5, D6, D7, D8, D10, D11 (Coffre personnel : `assetsInlineLimit` dès C1, `preload` de `personal-vault.html` et CSP appliquée sans Google à C4, `hosting.ignore` — rien d'autre), D12, D13, D14 (écrit § H ; en code, `APP_CHECK_HOSTS` de `measure-surfaces.mjs` est la seule exception au « 0 tierce »), D15 (hors dépôt, C3). D9 : non (constat par `find` : 375 copies « N » dans les sources le 16 septembre après C5, dont 124 sous `public/assets/IWC/derivatives/` ; `dist/` en compte 0 — un dossier vide `dist/assets 2` est réapparu pendant le build de `verify:v7` de C4, créé par la synchronisation après le passage du greffon : vide, couvert par `hosting.ignore`, J3 reste la seule parade). D2 : commande d'archivage remise à Jérôme (J3, avant le build de déploiement). D16 : C4 livré avant tout déploiement — **un seul déploiement groupé C1-C5 + relecture + C4** (J4), plus de « seconde invalidation » à décider ; la mesure de la relecture (C4 seul : 24 fichiers ≈ 204 ko brotli) reste utile comme ordre de grandeur de la cascade `index-*.css` (§ 4.1 bis : 7 fichiers sur les 41 vus par le relevé, `react-*.js` et `icons-*.js` conservés). D4 : reporté (§ 4.3).
- `test:v7` = `test:v6` + `test:performance-hygiene` + `node --test tests/performance-v7.test.mjs tests/hygiene-v7.test.mjs` + les 11 tests orphelins hors émulateur de la lecture barrières (`file-validation`, `generic-editing`, `generic-media-command`, `isolated-vault-configuration`, `media-download-wiring`, `media-presentation-runtime`, `pdf-export`, `personal-recovery`, `public-text-policy`, `registry-navigation`, `security-wave1` : 40 tests, ≈ 1,5 s, verts et gratuits — K8 appliqué en entier à la relecture, dette D24 close ; `interface-state` et le contrat sont déjà dans `test:v3`) ; `test:v7` += `tests/fonts-contract.test.mjs` à C4 (cinq tests : documents, feuilles, fichiers, CSP, `dist/` — ce dernier sauté quand `dist/` n'existe pas, donc dans `rm -rf dist && verify:v7` où le build vient après `test:v7` ; il s'exerce sur `npm run test:v7` après un build, vert le 16 septembre ; sur `dist/`, `verify:v7` tient les mêmes invariants par `measure:pf0` (`fonts.woff2 = 3`) et `measure:surfaces --check` (exactement 3 requêtes de police même origine sans échec : un `preload` orphelin — servi comme `index.html` par le repli — ou une police en `data:` change ce compte)) ; `verify:v7` = `test:v7` puis `audit:a11y` (construit `dist/`) puis `measure:pf0` puis `measure:surfaces --check` (`--fonts` retiré par C4 : réseau coupé sans exception, K1/K9). Seuils K9, marge +2 requêtes / +1 morceau sur le relevé fusionné : accueil ≤ 22 requêtes / ≤ 9 JS / exactement 3 polices même origine (C4), connexion ≤ 13 JS initiaux, démo ≤ 30 JS, Registre ≤ 18 JS, 0 icône seule, 0 requête tierce hors `APP_CHECK_HOSTS` sur chaque surface et chaque étape (C4) (témoin de la disparition totale du groupe `icons` : la signature « unique import `./createLucideIcon-*` » ne voit pas une icône sortie du groupe, qui importe `./icons-*` ; le contrôle effectif est `iconDefinitionFiles === 1` de `measure:pf0`, exécuté avant), étape Registre à chaud 0 JS / ≤ 3 requêtes. Contrat : test « V7 (fusion) » (scripts figés, 13 dépendances, relevé daté sans port, six exceptions axe « V8 » datées ; K13 appliqué en entier à la relecture : les groupes, le greffon, `hosting.ignore` ×2 et `numberedCopies` ne sont verrouillés que par `performance-v7`, qui importe la configuration réelle et exerce le greffon, `listFiles` et `measure:pf0` sur des dossiers témoins) ; `test:v6`, `audit:a11y`, `verify:v6` intacts à l'octet.
- Relevé `docs/audits/perf/2026-09-16.{json,md}` (mesuré par `verify:v7` sur le build fusionné) : `measure:pf0` tout vert ; `measure:surfaces --check` 0 dépassement ; ports éphémères retirés du relevé (les liens `modulepreload` injectés par Vite portaient l'origine locale) — seuls les temps (`readyAfterMs`, DCL, load, LCP) varient d'une exécution à l'autre.
- Barrière C5 : `tsc -b`, `lint`, `validate:ai`, `test:ui` 538 vitest (92 fichiers), node `test:v3` 182 · `test:v4` 46 · démo-publication 15 · `test:v5` 86 · `test:v6` 18 · hygiène 10 · `performance-v7` + `hygiene-v7` 12 · 11 orphelins 40 · contrat 34 tests (après relecture : `test:ui` 539 vitest, 93 fichiers) ; `audit:a11y` : 0 violation bloquante, 0 assertion en échec, 419 nœuds à vérifier (identique à V6), exceptions tolérées inchangées (12 `aria-required-children` sur `demo-value`, `region`, `heading-order`, `aria-allowed-role`) ; relevé `docs/audits/a11y/2026-09-16.{json,md}` régénéré par `verify:v7`, seul le motif des exceptions change (date du report D13) ; `git diff --check` propre ; `App.tsx` 3 471 lignes ; `dependencies` 13.
- Barrière C4 : `tsc -b`, `lint`, `validate:ai`, `rm -rf dist && verify:v7` vert (`test:ui` 539 vitest / 93 fichiers ; node 182 · 46 · 15 · 86 · 18 · 10 · 57 dont 1 sauté — `fonts-contract` (5) avant le build — · contrat ; `audit:a11y` 0 bloquante, 0 assertion en échec ; `measure:pf0` vert, `fonts.woff2 = 3` ; `measure:surfaces --check` 0 dépassement), `build:personal`, `git diff --check` ; `App.tsx` 3 471 lignes. **Relevé a11y avec les vraies polices** (le réseau coupé hors 127.0.0.1 servait Georgia/système jusqu'à C4) : 420 nœuds à vérifier contre 419, une seule différence — `mobile-390 demo-condition`, `color-contrast` × 11 au lieu de × 10. Analysée en rejouant axe sur la scène avec les polices servies puis refusées (404 sur `.woff2`, `scratchpad/v7/c4/a11y-diff-demo-condition.{mjs,json}`) : le nœud supplémentaire est `button:nth-child(1) > strong` (« Rolex Submariner · ensemble associé fictif », bouton d'un média, à 3 979 px sous la page), dont le libellé, composé en Archivo au lieu du repli, passe sur deux lignes (150 × 40 px) et se retrouve chevauché par un autre élément : axe ne détermine plus la couleur d'arrière-plan (canal `incomplete`, « à vérifier », pas une violation) ; 0 violation dans les deux cas, hors `region` × 3 déjà tolérées. Aucune exception ajoutée à `scripts/audit-accessibility.allowlist.json` (la liste ne couvre que les violations) ; le nœud rejoint les 419 autres « à vérifier » à la main, même motif (contraste indéterminé par chevauchement).
- Dettes consignées : contrôle mécanique de `until` ≥ vague courante dans la liste d'exceptions axe (D13 : le contrat vérifie seulement qu'aucune échéance ne reste « V7 ») ; polices (C4) : les 420 nœuds « à vérifier » d'axe ne sont pas figés (le contrat ne compare pas les relevés, seul l'effectif total est lu dans le `.md`) — une dérive silencieuse du canal `incomplete` reste possible ; `fonts.css` n'expose que le sous-ensemble latin (comme Google avant : `latin-ext`, cyrillique, grec, vietnamien n'étaient servis à personne en français ; un texte hors plage tombe sur Georgia / système) ; le `preload` ne vise que Newsreader (Archivo et JetBrains Mono sont découvertes par la feuille et demandées ≈ 20 ms après sa réception — 242 ms contre 112 ms pour Newsreader préchargée, latence émulée 100 ms, mesuré § 4.1 bis ; les précharger aussi ajouterait 66 ko à la file critique de toutes les surfaces sans gain sur le `h1`) ; `dist-personal/` reçoit encore les copies de `public/` (pas de greffon, D11 ; couvert au déploiement par `firebase.personal.json`, M10 — motif extglob à un ou plusieurs chiffres depuis la relecture ; un dossier « nom N/ » non vide reste livré par les deux gardes, comme par le greffon qui le conserve sciemment, cas jamais observé) ; `hosting.ignore` couvre aussi les copies nées dans `dist/` entre le build et le déploiement (4 résidus racine du dernier déploiement V6) ; risque R3 du préchargement (D10, formulaire vidé sur `vite:preloadError`) ; cause des copies « 2 » supposée (iCloud), à tarir hors dépôt (`.nosync` ou déplacement) ; résidu V-A2 (App Check sur toute surface) et les six exceptions axe : V8.

### 4.5 Étapes de Jérôme avant la recette (ordre)

1. **J1 — fait le 16 septembre** (D1) : trois `.woff2` déposés par Jérôme dans `src/assets/fonts/`, identiques à ceux que Google servait (tailles 34 940 / 31 340 / 131 848 o ; sha256 `7150c0ec5ad356453013d11affec1fbab95de0dd2dcecb043b4f1cb7f87c4ba4` Archivo, `2c32b9b3ee358c119e210f6f5195f9bd34894d78a785ff2e95d60e718e400af4` JetBrains Mono, `01817351be3edfc1714fe6d60ddea6a22a169a5ebd033b50c7f9495e5d9c386a` Newsreader ; `file` → « Web Open Font Format (Version 2) »), un `OFL.txt` par dossier (SIL OFL 1.1, aucun *Reserved Font Name* déclaré) ; C4 livré (`presentation-derivatives` réécrit, `--fonts` retiré, « 0 requête tierce » activé).
   **J2 — contrôle local fait par l'assistant, à confirmer par Jérôme en production après J4** (`scratchpad/v7/c4/j2-fonts-check.{mjs,json}`, `dist/` servi par `node:http` avec les en-têtes de `firebase.json` — CSP Report-Only comprise —, Chrome 152 headless, réseau coupé hors 127.0.0.1) : sur `/`, `/account/sign-in` et `/cartulary-demo#cover` à 1 280 et 390 px, `document.fonts.status === 'loaded'`, 3 faces `loaded` (Archivo, JetBrains Mono, Newsreader), `document.fonts.check('600 20px Newsreader')` et `('400 16px Archivo')` vrais, 0 requête vers un hôte `fonts.*`, 3 requêtes `.woff2` même origine (`content-type: font/woff2`, `cache-control: public, max-age=31536000, immutable`), aucun rapport CSP dans la console (seule erreur : `enterprise.js` d'App Check en DNS coupé, résidu V-A2) ; `h1` en Newsreader 500 sur l'accueil (`.public-hero h1`) et la connexion (`.account-access-intro h1`) ; dans le Cartulaire, le `h1` « Rolex Submariner » suit la règle globale `h1 { font-family: var(--font-headings) }` (Archivo 600, `index.css`, inchangée par C4) et le nom du modèle en Newsreader dans son propre élément — la coquille de chargement (`.application-shell h1`, Newsreader) a été capturée au vol. Captures PNG des `h1` aux deux largeurs (accueil, connexion, démo, coquille) dans `scratchpad/v7/c4/captures/` — Jérôme les compare à la production (même fichier Newsreader : aucune différence attendue) et recette le Coffre personnel (`personal-vault.html`) une fois.
2. **J3** — archivage des copies « 2 » (D2, simulation `-n` d'abord), avant le build de déploiement : le journal du build ne doit plus afficher « 124 copie(s) numérotée(s) retirée(s) ».
3. **R0** — mesure « avant » de V-B2 en production, Chrome réel, session démo (§ 4.2), consignée ici.
4. **J4** — feu vert : `rm -rf dist && npm run verify:v7 && ./node_modules/.bin/firebase deploy --only hosting --project studio-2614005370-a3e51` (CLI locale de `node_modules`, aucune CLI globale sur le poste ; déploiement groupé C1-C5 + relecture + C4, D16 : C4 est livré, plus de seconde invalidation à décider). Vérification post-déploiement (B5, § H de l'audit navigateur) : `curl -sI https://<site>/assets/newsreader-v26-latin-opsz-wght-DBQoWVJ5.woff2` → `content-type: font/woff2`, `cache-control: public, max-age=31536000, immutable` ; DevTools, cache vidé, accueil : 0 requête `fonts.googleapis` / `fonts.gstatic`, 3 `.woff2` même origine, `document.fonts.size === 3`, `h1` en Newsreader sans bascule visible, console sans rapport CSP ; Coffre personnel : mêmes polices sous la CSP appliquée.
5. **R1** (assistant, anonyme) puis **R2** (Jérôme, propriétaire et téléphone, objet de test neuf puis `npm run purge:test-cartulary`) → **R3 / C6** : colonnes de recette du § H des deux audits, § 5 de ce journal, `statut-49-constats.md` clos.

### 4.6 Relecture (16 septembre) : constats confirmés et corrections

Quatre angles (régression, mesures, déploiement, décisions-documents), chaque constat rejoué par deux vérificateurs sur des copies isolées de `5cc9587` ; corrigés ici ceux confirmés par les deux (ou courts et sûrs), un test par correctif. Aucun bloquant ; un important (M1).

| Point | Défaut | Correctif | Preuve |
|---|---|---|---|
| M1 (important) | « 320 000 dépassé depuis V3 » (commentaire `measure-pf0`, § 1, § 4.1) : faux — franchi à V5 point 1 (`c078b2d`), 330 028 est la taille V6 ; « mesuré 327 838 / 88 597 » venait du build exploratoire J | Commentaire, § 1, § 4.1 réécrits ; regex `tests/performance-v7.test.mjs` « cause datée » | builds datés rejoués : 367b9af 309 236, 248d96f 313 273, c078b2d 323 275, 109cc85 331 485, a4ce595 330 028 |
| F2 régr. / F3 dépl. | `hosting.ignore "**/* [0-9].*"` n'exclut qu'un chiffre : « nom 12.ext » livré (seule garde de `dist-personal/`) | `"**/* +([0-9]).*"` (extglob, équivalent du `\d+` du greffon) dans les deux configurations ; fixture « _DSC1019-3.768 12.webp » + faux positif « logo 2x.png » dans le test `listFiles` | `listFiles` de firebase-tools 15.27 : x 2 / x 12 / x 123 exclus, « logo 2x.png » et « rapport 2 » conservés |
| F4 régr. | `iconsImports` et `reactChunk` (modulepreload) de `measure:pf0` sans témoin rouge (mutants survivants) | Deux fixtures rouges dans le test « D6 / K7 » | mutants « iconsImports: true » et « reactChunk: length === 1 » : témoins verts sous mutant, rouges sur le script réel |
| F6 régr. / F1, F3 déc. | Test « V7 (fusion) » re-verrouillait vite.config.ts, firebase*.json et measure-pf0 par regex de forme (K13 non appliqué) ; message `performance-v7:59` « partagée avec hygiene-v7 » faux | Bloc réduit à package.json + relevé + allowlist ; assertion redondante de nom retirée (le greffon est retrouvé par son nom l. 54) | mutants M16/M23/M30/M31 tués par `performance-v7` seul ; reformatage de `closeBundle` ne rougit plus rien |
| F2 déc. | K8 cité appliqué alors que les 11 orphelins restaient dehors | Ajoutés à `test:v7` et au contrat | 40/40 verts, ≈ 1,5 s, sans émulateur |
| F12 déc. | Verrou (b) purement textuel : un prédicat `isWatchWebsite` cassé conservant la ligne passait | `tests/ui/watch-website-follow-up.test.tsx` rend App sur `/watch-website?publicCode=…` (0 écoute, 0 lecture des suivis) | mutant `'/watch-site'` : `observe` appelé 1 fois → rouge ; HEAD vert |
| M2, M4, M5, F7/M8, F8, M3/F9, F2 dépl., F5 dépl., F4 déc., M6, F1 dépl. | Chiffres ou formulations du journal : 18 icônes (36 morceaux / 45 icônes), gzip à deux niveaux, « rendu prêt » de deux instruments, 22 importateurs (21), coût du groupe icons sur les surfaces légères, conditions du 0 JS, ≈ 281 ko (204 / 272), `firebase` global, tableau des commits, portée du témoin « 0 icône seule », risque R3 | § 1, § 2 (D8, D10), § 4, § 4.1, § 4.4, § 4.5 corrigés ; commentaire de `measure-surfaces.mjs` ; consigne « attendre ≥ 3 s » en R1 (§ H B2) | rejeux des vérificateurs (scratchpad `v7/review/verify-*`) |

Écartés (motif) : F1 déploiement en code (ignorer `vite:preloadError` pendant le préchargement) — décision 9 / R3 consignée « accepter en V7, neutraliser si observé en recette », repli V8 écrit à D10 ; F3 déploiement, dossiers « nom N/ » non vides — le greffon les conserve sciemment (testé), les deux gardes restent alignées, cas jamais observé ; F8 décisions (pointer `statut-49-constats.md` hors dépôt) — forme prescrite par D12 (§ H est le tableau versionné, 49/49 statuts identiques), convention des journaux V2-V7 ; F9 décisions (seuils et requête refusée dans le `.md`) — le `.md` est un résumé (JSON = relevé complet, contrat K13), seuils écrits § 4.4 et figés par test, la requête refusée est nommée § 4.1 ; F10 décisions (figer rule/scenes/target des six exceptions) — mutant équivalent sur le build livré (relevé a11y identique à l'octet), figer des données au contrat va contre K13 ; M6 en code (élargir la signature « icône seule ») — le correctif proposé classe `icons-*.js` lui-même en icône seule, documenté à la place ; M9 — trois builds distincts, absorbé par M1.

## Relecture V6 — corrections (commit 0ea7fb9 « V6 relecture : … »)

| Point | Défaut | Correctif | Preuve |
|---|---|---|---|
| F1 mesures (bloquant → important) | `scripts/audit-accessibility.mjs` comparait `scrollWidth` à `innerWidth` ; en émulation mobile Chrome gonfle `innerWidth` jusqu'à la largeur du contenu (× 4) : tout débordement < 1 560 px passait, le « 390/390 » du relevé n'était pas vérifié | CHECKS relève `documentElement.clientWidth` ; assertion « scrollWidth ≤ clientWidth » ; `mobile: true` conservé (contrat l. 972) | dist + div 420 px → 16 assertions « 421/390 » en échec, code 1 ; régression réelle V-D4 (D6 (a) annulé) → « demo-value 421/390 », code 1 ; build sain 390/390 et 1440/1440, code 0 ; sonde CDP : div 420 px → innerWidth 421, clientWidth 390 |
| F2 mesures / F1 outillage (important) | Canal `incomplete` d'axe réduit à des identifiants (`resultTypes: ['violations']`, tronqué à un nœud par règle) et tu par le .md ; 13-20 nœuds color-contrast (serious) par scène démo indéterminés depuis les dégradés d'indice V6 (`.page-tabs__inner`, `.market-chart-card`) | `resultTypes: ['violations', 'incomplete']` ; nœuds comptés par scène et par règle (`toReview`, `incomplete[]` : effectif, premier sélecteur, motif d'axe) ; colonne « À vérifier (incomplete) », section « Nœuds à vérifier », total dans le résultat ; `NEVER_INCOMPLETE` (aria-prohibited-attr) = assertion en échec ; contrastes des onglets (`--ink`/`--muted` sur `--paper` : 15,83:1 / 4,82:1) et des dates du graphique (`--muted` sur `--sheet`) figés au contrat, axe ne les mesurant plus. D15 (a) inchangé : le canal ne change pas le code de sortie (assertion du contrat) | Relevé regénéré : 419 nœuds listés (accueil 30 × 3 pseudo-élément antérieur, démo 8-65 dégradé/glyphes ✓, `th-has-data-cells` × 2 sur la page 05) ; mutant M1 (carrousel sans `role="group"`) → 2 assertions aria-prohibited-attr en échec, code 1 |
| F2 outillage (important) | Aucune identité de scène : page 404 servie à la place de `/accessibilite` ou écran d'erreur (`ready=false`) audités « sans erreur » ; saut « déclencheur absent » accepté partout | `title` attendu par scène (`document.title` exact), assertion « page rendue » sur `ready`, `openAbsentOn: 'desktop-1440'` sur le seul menu de l'accueil, sinon assertion « déclencheur présent » en échec | M6 (route retirée) → « titre attendu (Page introuvable · Cartularia) » × 2, code 1 ; M7 (exception) → « page rendue » + titre × 2, code 1 ; build sain : 32 titres conformes (accueil « Cartularia · Le dossier vivant… », démo « Cartulaire Rolex Submariner · Cartularia », service « Accessibilité · Cartularia » etc.) |
| F4 outillage (mineur) | `date` à la seconde et `base` avec port éphémère dans un JSON suivi : 2 lignes de bruit par exécution | `report.date = day` (jour local, déjà dans le nom du fichier), `base` retiré ; suivi du JSON conservé (D15 (a), contrat l. 973) | Trois exécutions (dont une après rebuild) → JSON et .md identiques à l'octet ; contrat : `survey.base === undefined`, date `AAAA-MM-JJ` |
| REG-1 (mineur) | `useRevealActiveTab(activePage)` appelé avant la porte de chargement du mini-site : la piste n'existe qu'après la projection, `activePage` inchangé, l'effet ne rejoue pas (lien profond `#condition` rogné de 2-32 px sous 390 px) | Appel déplacé sous `publicProjectionLoading` : `useRevealActiveTab(isWatchWebsite && publicProjectionLoading ? null : activePage)` ; assertion de placement du contrat mise à jour | Test jsdom porte → piste : 1 appel ; contre-épreuve valeur constante : 0 (l'ancien placement échoue) ; mesures CDP des relecteurs sur ce même correctif : 1 appel après la porte |
| REG-2 (mineur, dette fe3aa90 antérieure à V6) | Trois comportements V5 déplacés par B1 sans verrou | Trois fragments courts au contrat registre (test « V-D5 — DOM de la coque »), pas de regex « ligne entière » (forme écartée par le brief) | Mutants R3 (`!securityHref`), R4 (`===`), R5 (garde retirée) tués ; 6/6 verts |
| REG-3 (mineur) | `background-repeat: no-repeat`, caches opaques et `content: ' ·' / ''` non figés (C4, C5, C9, C16 survivants) | Quatre assertions (piste, carte mobile, `.registry-product-label span::after`) | Contrats verts sur HEAD ; chaînes mutées rejetées |
| F3 mesures (important / mineur) | `#value` déborde à 844 × 390 et 768 (document 999 px) : `.comparable-group` élément de grille à taille minimale automatique (`.comparables-table > div { min-width: 965px }` de base, la règle du bloc 1 023 n'est qu'un doublon) | `.comparable-group { min-width: 0 }` en règle de base (couvre aussi 1 024-1 063 px où la carte dépassait son conteneur) ; assertion au contrat | CDP : 844 → 844/844 (carte 780, tableau 778/965 défilant), 768 → 768/768, 998 → 998/998, 1 024 → 1 024/1 024, 1 280 et 390 inchangés |
| F7 mesures (mineur, antérieur à V6) | `window.scrollTo({ behavior: 'smooth' })` explicite (App.tsx ×2, GenericCartularyView.tsx:104) ignoré par `scroll-behavior: auto !important` (CSSOM View ne consulte la feuille que pour `auto`) | `pageScrollBehavior()` dans `interfaceState.ts` (préférence lue à l'appel, `instant`/`smooth`, jsdom sans `matchMedia` toléré) ; App.tsx 3 471 lignes (plafond 3 473, D14 (b)) ; Registre toujours `instant` | Test node (3 cas) ; CDP : sous `reduce`, 777 → 0 en 6 ms, 0 image intermédiaire ; sans préférence 16 images, 493 ms (inchangé) |

Barrière : tsc -b, lint, validate:ai, test:ui (91/524), test:v6, build sans émulateurs, git diff --check, audit:a11y (0 bloquante, 0 assertion en échec, 419 nœuds à vérifier) ; relevé `docs/audits/a11y/2026-09-16.*` regénéré et reproductible. Pièces : scratchpad `v6/review/correcteur/` (fix.json mesures CDP, out-{overflow,regress,m6,m1,m7}, out-idem, audit-*.log, mutants.txt).

Écartés (à consigner V7) :
- F4 mesures (mineur, antérieur à V6, hors périmètre 390 × 844) : barre du Cartulaire + onglets collants = 31,8 % de la hauteur en paysage 844 × 390 (34,4 % à 780 × 360) ; `position: 'sticky'` est un style inline (`BarreDossier.tsx:149-151`), une règle `@media (max-height: 500px)` ne suffit pas sans `!important` ou retouche TSX — hors « court et sûr ».
- F5 mesures (sans effet / mineur) : Tab sur un onglet partiellement visible ne défile pas (Blink `rect_partial = kNoScroll`, commun aux trois moteurs) ; conforme à D4 (b), 2.4.11 AA respecté, résidu 2.4.12 AAA ; `scroll-padding` sans effet, l'écouteur `focusin` doublerait le défilement au clic.
- F6 mesures (mineur / sans effet) : `.todo-action` 36 × 36 dans le popover À faire ; équivalent ≥ 44 px (`.todo-trigger` visible et actif, Échap, toucher hors) ; actions d'édition (36 px, `.todo-list__status` 28 px, grille `.todo-edit-form` 36px 36px) non mesurables sans session — relevé V7 « actions internes du Cartulaire en édition ».
- Observation hors constat : à 844 × 390 le graphique `.market-bars` (379 px) dépasse sa carte (378 px) sans élargir le document (bloc ≤ 1 023 sans défilement interne) — même famille que le report « tablette » de coherence.md § 7 ; contraste des numéros d'onglets sous les 24 px d'ombre (3,46:1 pire cas, libellés ≥ 11:1) et état `:hover` (`--muted` sur `--fill` 4,24:1) restent à trancher en V7.